## 1. CSS Styles Additions

- [x] 1.1 Append the missing cart view, form cards, category cards grid, and staff modal CSS rules to `app/globals.css`.

## 2. Customer Cart Layout Refactoring

- [x] 2.1 Refactor `components/DigitalMenu.tsx` (cart sheet view) to apply correct layout selectors: `.subhead`, `.cartlist`, `.citem`, `.qmini`, `.formcard`, and `.totrow`.

## 3. Admin Menu & Category Cards Refactoring

- [x] 3.1 Refactor `components/MenuCategoryCRUD.tsx` category tab to render categories inside `.catgrid` using `.catmcard` panels.
- [x] 3.2 Refactor category and dish modal overlays in `components/MenuCategoryCRUD.tsx` to use the `.smodal` overlays and `.dfield` layout structures.

## 4. Verification & Testing

- [x] 4.1 Run `npm run test` to confirm all tests pass successfully.
- [x] 4.2 Run `npm run test:coverage` to confirm coverage remains high.
