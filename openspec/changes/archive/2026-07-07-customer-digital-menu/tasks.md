## 1. Setup & Data Loading

- [x] 1.1 Create typescript interfaces for `Category` and `Dish` tables
- [x] 1.2 Update `app/page.tsx` to run as an async Server Component querying `categories` and `dishes` tables from Supabase

## 2. Digital Menu Component & Layout

- [x] 2.1 Implement the primary customer client component in `components/DigitalMenu.tsx`
- [x] 2.2 Replicate the hero banner styling and table indicator from `prototipo.html`
- [x] 2.3 Create the sticky category selector rail with click-to-scroll category navigation

## 3. Dish Cards Grid & Details Overlay

- [x] 3.1 Build the dish card layout showing the thumbnail (emoji on tone-colored HSL gradient background), description, price, and a styled "sold out" overlay for unavailable dishes
- [x] 3.2 Implement the bottom drawer modal sheet showing the selected dish details with open/close toggles

## 4. Verification

- [x] 4.1 Write a Vitest unit test suite to verify categories grouping and modal interactions
- [x] 4.2 Verify manual loading, category jump, and modal trigger rendering in the browser
